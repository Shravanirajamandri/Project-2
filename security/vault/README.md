Never commit real passwords or cloud credentials.

Production pattern:
Application -> identity/secret provider -> short-lived or managed secret -> application

The Kubernetes Secret in this lab contains only demo credentials.
